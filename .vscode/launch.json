{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for rdebug-ide",
      "type": "Ruby",
      "request": "attach",
      "remoteHost": "localhost",
      "remotePort": "9000",
      "remoteWorkspaceRoot": "/usr/src/app", //"${workspaceRoot}"
    }
  ]
}
